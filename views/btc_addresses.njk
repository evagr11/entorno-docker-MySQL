{% extends 'base.njk' %}

{% block content %}
<h1>{{ title }}</h1>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Dirección</th>
        </tr>
    </thead>
    <tbody>
        {% for address in addresses %}
        <tr>
            <td>{{ address.id }}</td>
            <td>{{ address.address }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}